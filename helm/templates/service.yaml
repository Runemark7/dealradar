apiVersion: v1
kind: Service
metadata:
  name: {{ include "crawl4aitest.fullname" . }}
  labels:
    {{- include "crawl4aitest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "crawl4aitest.selectorLabels" . | nindent 4 }}
